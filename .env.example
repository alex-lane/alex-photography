# Build-time public var (safe to expose). Copy to `.env`.
# Cloudflare Turnstile TEST site key (always passes) — replace with your real
# site key for production. Set the same value in Cloudflare Pages build settings.
PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
